# Azure Cosmos DB Data Modeling

11 rules (CRITICAL impact) for document schema design: embedding vs referencing, size limits, schema versioning, type discriminators, serialization, and denormalization.

Part of the [cosmosdb-agent-kit](https://github.com/AzureCosmosDB/cosmosdb-agent-kit) skill collection.

## Installation

**Claude Code:**
```bash
cp -r skills/cosmosdb-data-modeling ~/.claude/skills/
```

**GitHub Copilot / VS Code:**
```bash
cp -r skills/cosmosdb-data-modeling ~/.copilot/skills/
```
