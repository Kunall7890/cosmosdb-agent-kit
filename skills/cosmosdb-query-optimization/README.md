# Azure Cosmos DB Query Optimization

12 rules (HIGH impact) for query performance: point reads, projections, pagination, parameterization, filter ordering, and cross-partition avoidance.

Part of the [cosmosdb-agent-kit](https://github.com/AzureCosmosDB/cosmosdb-agent-kit) skill collection.

## Installation

**Claude Code:**
```bash
cp -r skills/cosmosdb-query-optimization ~/.claude/skills/
```

**GitHub Copilot / VS Code:**
```bash
cp -r skills/cosmosdb-query-optimization ~/.copilot/skills/
```
