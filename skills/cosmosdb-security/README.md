# Azure Cosmos DB Security

5 rules (HIGH impact) for security: disabling keys, managed identity, network restrictions, RBAC least privilege, and continuous backup.

Part of the [cosmosdb-agent-kit](https://github.com/AzureCosmosDB/cosmosdb-agent-kit) skill collection.

## Installation

**Claude Code:**
```bash
cp -r skills/cosmosdb-security ~/.claude/skills/
```

**GitHub Copilot / VS Code:**
```bash
cp -r skills/cosmosdb-security ~/.copilot/skills/
```
