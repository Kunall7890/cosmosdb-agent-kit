# Azure Cosmos DB Partition Key Design

8 rules (CRITICAL impact) for partition key selection: cardinality, hotspot avoidance, hierarchical keys, synthetic keys, and size limits.

Part of the [cosmosdb-agent-kit](https://github.com/AzureCosmosDB/cosmosdb-agent-kit) skill collection.

## Installation

**Claude Code:**
```bash
cp -r skills/cosmosdb-partition-key ~/.claude/skills/
```

**GitHub Copilot / VS Code:**
```bash
cp -r skills/cosmosdb-partition-key ~/.copilot/skills/
```
