# Azure Cosmos DB Vector Search

6 rules (HIGH impact) for vector search: embedding policies, index types, normalization, VectorDistance queries, and RAG repository patterns.

Part of the [cosmosdb-agent-kit](https://github.com/AzureCosmosDB/cosmosdb-agent-kit) skill collection.

## Installation

**Claude Code:**
```bash
cp -r skills/cosmosdb-vector-search ~/.claude/skills/
```

**GitHub Copilot / VS Code:**
```bash
cp -r skills/cosmosdb-vector-search ~/.copilot/skills/
```
