# Azure Cosmos DB Indexing Strategies

7 rules (MEDIUM-HIGH impact) for indexing policies: path exclusions, composite indexes, spatial indexes, index types, and indexing modes.

Part of the [cosmosdb-agent-kit](https://github.com/AzureCosmosDB/cosmosdb-agent-kit) skill collection.

## Installation

**Claude Code:**
```bash
cp -r skills/cosmosdb-indexing ~/.claude/skills/
```

**GitHub Copilot / VS Code:**
```bash
cp -r skills/cosmosdb-indexing ~/.copilot/skills/
```
